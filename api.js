//let apiUrl = "http://localhost:8000/"
let apiUrl = "https://minga-back-58at.onrender.com/"

/* if (process.env.NODE_ENV === "production") {
    apiUrl = import.meta.env.VITE_API
} */

export default apiUrl